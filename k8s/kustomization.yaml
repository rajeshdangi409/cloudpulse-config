apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cloudpulse

# Applied in order by Flux (and kubectl). Namespace first so the
# Deployment and Service have somewhere to live.
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
